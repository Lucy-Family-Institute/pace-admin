- args:
    sql: CREATE TABLE "public"."classifications"("id" serial NOT NULL, "name" text
      NOT NULL, "identifier" text NOT NULL, "scheme" text NOT NULL, PRIMARY KEY ("id")
      , UNIQUE ("id"));
  type: run_sql
- args:
    name: classifications
    schema: public
  type: add_existing_table_or_view
