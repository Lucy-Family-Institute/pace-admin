- args:
    sql: CREATE TABLE "public"."persons_publications"("id" serial NOT NULL, "person_id"
      integer NOT NULL, "publication_id" integer NOT NULL, PRIMARY KEY ("id") , FOREIGN
      KEY ("person_id") REFERENCES "public"."persons"("id") ON UPDATE no action ON
      DELETE no action, FOREIGN KEY ("publication_id") REFERENCES "public"."publications"("id")
      ON UPDATE no action ON DELETE no action);
  type: run_sql
- args:
    name: persons_publications
    schema: public
  type: add_existing_table_or_view
