- args:
    name: org_reviews
    table:
      name: users
      schema: public
    using:
      manual_configuration:
        column_mapping:
          id: user_id
        remote_table:
          name: org_reviews
          schema: public
  type: create_array_relationship
