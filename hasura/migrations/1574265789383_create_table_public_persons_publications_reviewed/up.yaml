- args:
    sql: CREATE TABLE "public"."persons_publications_reviewed"("id" serial NOT NULL,
      "persons_publications_id" integer NOT NULL, "reviewed_id" integer NOT NULL,
      PRIMARY KEY ("id") , FOREIGN KEY ("persons_publications_id") REFERENCES "public"."persons_publications"("id")
      ON UPDATE no action ON DELETE no action, FOREIGN KEY ("reviewed_id") REFERENCES
      "public"."reviewed"("id") ON UPDATE no action ON DELETE no action);
  type: run_sql
- args:
    name: persons_publications_reviewed
    schema: public
  type: add_existing_table_or_view
