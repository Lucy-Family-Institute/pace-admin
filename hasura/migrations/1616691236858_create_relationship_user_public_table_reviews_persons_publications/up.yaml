- args:
    name: user
    table:
      name: reviews_persons_publications
      schema: public
    using:
      manual_configuration:
        column_mapping:
          user_id: id
        remote_table:
          name: users
          schema: public
  type: create_object_relationship
