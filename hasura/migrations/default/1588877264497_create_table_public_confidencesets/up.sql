
CREATE TABLE "public"."confidencesets"("id" serial NOT NULL, "persons_publications_id" integer NOT NULL, "value" float8 NOT NULL, "datetime" timestamptz NOT NULL DEFAULT now(), PRIMARY KEY ("id") , FOREIGN KEY ("persons_publications_id") REFERENCES "public"."persons_publications"("id") ON UPDATE no action ON DELETE restrict, UNIQUE ("id"));